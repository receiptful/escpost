export function CalibrationPage() {
  return (
    <section aria-labelledby="calibration-heading" class="space-y-4">
      <div>
        <p class="text-sm font-semibold text-primary">Workbench</p>
        <h1 id="calibration-heading" class="mt-1 text-3xl font-bold">Calibration</h1>
      </div>
      <p class="max-w-2xl text-base-content/70">
        Calibration controls are not available in the workbench yet.
      </p>
    </section>
  );
}
