{
  "name": "escpost-browser-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "build": "rm -rf dist && bun run build:chrome && bun run build:firefox",
    "build:chrome": "ESCPOST_BROWSER=chrome ESCPOST_ENTRY=bridge vite build && ESCPOST_BROWSER=chrome ESCPOST_ENTRY=popup vite build && node scripts/build-manifest.mjs chrome",
    "build:firefox": "ESCPOST_BROWSER=firefox ESCPOST_ENTRY=background vite build && ESCPOST_BROWSER=firefox ESCPOST_ENTRY=relay vite build && ESCPOST_BROWSER=firefox ESCPOST_ENTRY=popup vite build && node scripts/build-manifest.mjs firefox",
    "test": "bun run build && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "0.1.24",
    "happy-dom": "20.0.0",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.4"
  }
}
