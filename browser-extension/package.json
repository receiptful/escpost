{
  "name": "escpost-browser-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@escpost/browser": "workspace:*"
  },
  "scripts": {
    "build": "rm -rf dist && ESCPOST_ENTRY=background vite build && ESCPOST_ENTRY=relay vite build && ESCPOST_ENTRY=popup vite build && node scripts/build-manifest.mjs"
  }
}
