{
  "name": "escpost-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && ESCPOST_ENTRY=background vite build && ESCPOST_ENTRY=relay vite build && ESCPOST_ENTRY=auth-bridge vite build && ESCPOST_ENTRY=ws-patch vite build && ESCPOST_ENTRY=qz-shim vite build && ESCPOST_ENTRY=popup vite build && ESCPOST_ENTRY=settings vite build && ESCPOST_ENTRY=welcome vite build && node scripts/build-manifest.mjs && mkdir -p dist/icons && cp src/ui/icons/*.png dist/icons/ && cp src/ui/theme.css src/popup/popup.html src/popup/popup.css src/settings/settings.html src/settings/settings.css src/welcome/welcome.html dist/",
    "test": "vitest run"
  }
}
